---
import BaseLayout from '../layouts/BaseLayout.astro';

// Formspree form ID - replace with actual ID after setup
const formspreeId = 'mgozanjk';
---

<BaseLayout title="Submit a Tool" description="Submit a tool to be listed in the Solo Founder Tools directory">
  <section class="py-12">
    <div class="max-w-2xl mx-auto px-4">
      <h1 class="text-3xl font-bold text-gray-900 mb-2">Submit a Tool</h1>
      <p class="text-gray-600 mb-8">
        Know a tool that solo founders should know about? Submit it for review.
      </p>

      <form
        action={`https://formspree.io/f/${formspreeId}`}
        method="POST"
        class="space-y-6"
      >
        <div>
          <label for="tool_name" class="block text-sm font-medium text-gray-700 mb-1">
            Tool Name *
          </label>
          <input
            type="text"
            id="tool_name"
            name="tool_name"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
            placeholder="e.g., Stripe, Vercel, Notion"
          />
        </div>

        <div>
          <label for="website" class="block text-sm font-medium text-gray-700 mb-1">
            Website URL *
          </label>
          <input
            type="url"
            id="website"
            name="website"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
            placeholder="https://example.com"
          />
        </div>

        <div>
          <label for="category" class="block text-sm font-medium text-gray-700 mb-1">
            Category *
          </label>
          <select
            id="category"
            name="category"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
          >
            <option value="">Select a category</option>
            <option value="hosting">Hosting & Infrastructure</option>
            <option value="payments">Payments & Billing</option>
            <option value="email">Email & Communication</option>
            <option value="analytics">Analytics & Monitoring</option>
            <option value="support">Customer Support</option>
            <option value="marketing">Marketing & SEO</option>
            <option value="design">Design & Assets</option>
            <option value="productivity">Productivity</option>
            <option value="auth">Authentication</option>
            <option value="databases">Databases</option>
            <option value="other">Other</option>
          </select>
        </div>

        <div>
          <label for="description" class="block text-sm font-medium text-gray-700 mb-1">
            Why should we list this tool?
          </label>
          <textarea
            id="description"
            name="description"
            rows="4"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
            placeholder="What makes this tool great for solo founders?"
          ></textarea>
        </div>

        <div>
          <label for="email" class="block text-sm font-medium text-gray-700 mb-1">
            Your Email (optional)
          </label>
          <input
            type="email"
            id="email"
            name="email"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
            placeholder="We'll notify you when it's listed"
          />
        </div>

        <div>
          <label class="flex items-center gap-2">
            <input type="checkbox" name="affiliated" class="rounded border-gray-300 text-primary-600 focus:ring-primary-500" />
            <span class="text-sm text-gray-600">I work for or am affiliated with this tool</span>
          </label>
        </div>

        <button type="submit" class="btn-primary w-full justify-center">
          Submit Tool
        </button>
      </form>

      <div class="mt-8 p-4 bg-gray-50 rounded-lg">
        <h2 class="font-semibold text-gray-900 mb-2">What happens next?</h2>
        <p class="text-sm text-gray-600">
          We review all submissions manually. If your tool fits our criteria, we'll add it to the directory within a few days. Tools must be relevant to solo founders and have clear pricing information.
        </p>
      </div>
    </div>
  </section>
</BaseLayout>
